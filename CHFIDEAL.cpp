#include<iostream>
#include<thread>
#include<chrono>
using namespace std;
int main()
{
  cout<<1<<" "<<flush;
  int y;
  scanf("%d",&y);
   if(y==2)
   cout<<"3\n"<<" "<<flush;
   else
   cout<<"2\n"<<" "<<flush;
}